import ReturnsPage, { metadata as returnsMetadata } from '@/app/returns/page'

export const metadata = {
  ...returnsMetadata,
  title: 'Refund Policy — ATMODESK Bangladesh',
  description: 'Official refund policies, processing times, and return details for ATMODESK Bangladesh. Processed via Messenger.',
}

export default ReturnsPage
